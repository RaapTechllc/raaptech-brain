# Mine pass: Gate agent writes accepts; Conflicts parked for Hermes audit

We need Insights from Hermes memories and the Obsidian vault without dumping stale residue into the brain. We decided Miners never write domain files; a Gate agent alone accepts via `brain save` using a four-part Accept rule; Conflicts are parked and never auto-overwrite; only a Hermes audit may resolve Conflicts by writing. Alternatives rejected: human-only gate (slower, skips day-one judgment), auto-merge on newer source (reintroduces flopped facts), and full workstation crawl (mess).
